# API Scraper
This project collects articles from Open Platform API based on a search query and date range. 
The script handles pagination, extracts relevant article information, and saves the final dataset as a CSV file for analysis.


## Requirements

- Python

- `requests`

- `pandas`
